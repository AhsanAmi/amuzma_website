import { CncMachines } from "../pages/CncMachines";
import { createPageMetadata } from "../lib/pageMetadata";

export const metadata = createPageMetadata("cnc-machines");

export default CncMachines;
