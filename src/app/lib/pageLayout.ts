/** Shared content width aligned with the desktop/mobile header.
 * Matches Header: px-4 sm:px-6 lg:px-10 xl:px-16 2xl:px-24
 * so page text starts under "Company" and ends at the language chevron.
 * Do not use on full-bleed banners/heroes.
 */
export const PAGE_CONTAINER =
  "w-full px-4 sm:px-6 lg:px-10 xl:px-16 2xl:px-24";

/** Offset for in-page section anchors under sticky header + product section nav. */
export const SECTION_SCROLL_MARGIN =
  "scroll-mt-[64px] lg:scroll-mt-[calc(92.16px+3.25rem)]";
