import { MissionVision } from "../pages/MissionVision";
import { createPageMetadata } from "../lib/pageMetadata";

export const metadata = createPageMetadata("mission-vision");

export default MissionVision;
